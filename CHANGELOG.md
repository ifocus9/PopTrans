# 更新日志 (Changelog)

所有关键变更都将记录在此文件中。本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 规范与 [Semantic Versioning](https://semver.org/lang/zh-CN/) 语义化版本规范。

---

## [v1.3.0] - 2026-09-23

### 🚀 重点新增与硬件加速 (Added)

- **GPU 硬件加速推理支持**：
  - 设置界面新增「推理硬件加速」选项，支持「自动检测」、「GPU 显卡加速」与「纯 CPU 推理」三种模式。
  - 自动检测系统显卡环境，有独立显卡时优先使用显卡加速推理（速度大幅提升），无独显或驱动异常时平滑回退 CPU 推理，兼顾极致性能与稳定性。
  - 新增 `scripts/build_ai_engine_gpu.bat` 专用 GPU 打包脚本，打通 Go 宿主热重启引擎与实时配置生效。
- **模型下载转入后台静默运行**：
  - 首次启动若无本地模型，弹窗提供「转入后台静默下载」按钮，支持随时收起到托盘。
  - 支持按 <kbd>Esc</kbd> 键或右上角关闭按钮快速收起到托盘后台下载，托盘实时显示下载百分比，下载完成后自动就绪，不突兀弹窗打扰用户。
  - 完善断点续传机制，下载中断时数据安全落盘，下次启动无缝继续下载。
- **翻译结果弹窗点击外部自动关闭**：
  - 选词翻译结果弹窗支持点击窗口外部任意区域（桌面、浏览器、任务栏等）即时、自动收起。
  - 采用 Windows 原生全局鼠标物理点击检测与前台穿透激活双保险，彻底突破操作系统前台焦点锁定（Focus Lockout）限制，实现 100% 灵敏稳定的失焦自动关闭。

### ⚡ 体验与交互优化 (Changed)

- **启动加载弹窗智能动作呈现**：精准判断底层服务状态，仅在系统真正处于大模型下载过程中时才显示「转入后台静默下载」按钮与提示，在常规本地模型加载及服务自检阶段保持纯粹简洁的启动反馈。
- **标题栏拖拽保护**：精准识别鼠标按下起点（Down Edge），按住标题栏拖动移动窗口或在窗口内复制/划选时完全豁免外部点击判定，无论如何快速拖动，窗口绝不误消失。
- **全新启动准备窗口设计**：重构启动弹窗，增加可拖拽标题栏、平滑百分比进度条与全套精致微光悬浮按钮，支持鼠标任意拖动。

### 🐛 缺陷修复 (Fixed)

- **焦点抢占彻底根治**：修复模型下载进度刷新时（每 750ms）由于显隐逻辑判断缺陷导致窗口被反复强行调用 `WindowShow` 抢占 Windows 前台焦点的核心体验 Bug。
- **托盘退出死锁与进程残留**：
  - 重构后台 `Supervisor` 并发模型，移除了长达 30 分钟轮询中的互斥死锁大锁，引入瞬时打断信号 `stopCancel`。
  - 彻底解决模型下载或启动等待期间从托盘点击「退出」导致主线程死锁、`PopTrans.exe` 僵死在后台以及后台引擎无法正常退出的问题。
  - 退出时使用进程树强制终结，确保退出后后台无任何残留进程，杜绝杀死后又被宿主异常拉起的现象。
- **按钮样式穿透与错位**：修复启动弹窗关闭按钮换行掉到第二行以及 WebView2 原生按钮白底灰框穿透的视觉瑕疵，全局规范 `.icon-button` 重置规则。

---

## [v1.2.0] - 2026-08-31

### ✨ 新增功能 (Added)

- **文本翻译工作台**：新增独立双卡片翻译界面，支持手动输入长文本离线翻译、实时字符统计与一键复制译文。
- **双击托盘打开**：系统托盘支持双击图标直接呼出文本翻译工作台，右键托盘菜单同步提供快捷入口。
- **快捷键**：工作台输入框支持 <kbd>Ctrl + Enter</kbd> 快捷键即刻提交翻译。
- **设置页返回入口**：设置界面新增顶部左侧返回图标按钮（<kbd>←</kbd>）与底栏「返回」按钮，方便随时返回翻译界面。
- **自动清空重置**：窗口关闭后自动清空工作台原文输入与译文结果，每次呼出均呈现干净的初始就绪状态。

### ⚡ 优化与改进 (Changed)

- **主窗口导航结构**：移除分段 Tab 切换栏，改为主窗口标准标题与右上角轻量化设置图标按钮（<kbd>🎛</kbd>）。
- **设置保存机制**：点击「保存」后配置与快捷键即时生效并显示反馈提示，保存后保持弹窗开启，不中断用户工作流。
- **精简托盘菜单**：移除托盘菜单中冗余的独立设置入口，统一由翻译工作台右上角直达。
- **按钮视觉统一**：底栏「返回」按钮与「保存」按钮规格、高度、圆角和动效完全统一。

### 🐛 问题修复 (Fixed)

- **生命周期唤醒**：修复主窗口关闭隐藏后，再次通过托盘双击或菜单无法重新弹出的状态同步问题。
- **排版对齐**：彻底修复模型输出前置换行符与 HTML 模版缩进导致的译文区域顶部和左侧边距过大问题。
- **双主题对比度**：修复浅色模式下主标题文字、Logo 图标与引擎就绪徽标在浅色背景下不可见的问题。

---

## [v1.0.0] - 2026-08-30

### ✨ 核心功能

- **划词快捷翻译**：全局快捷键一键捕获选中文本，并在光标附近弹出毛玻璃翻译结果卡片。
- **离线 OCR 截图翻译**：原生 Go 框选截图，集成 RapidOCR 离线多倍率识别与自动重试。
- **纯 CPU 离线大模型推理**：内嵌腾讯 Hy-MT2-1.8B GGUF 翻译大模型，基于 llama.cpp 实现纯 CPU 离线推理，零 GPU/联网依赖。
- **UI 单实例与进程守护**：Wails 亚克力毛玻璃界面，支持暗黑/明亮双主题自适应，支持 AI 引擎空闲自动回收与按需拉起。
- **本地 API 开放**：提供 OpenAI 兼容的 `/v1/chat/completions` 与 OCR 接口。
